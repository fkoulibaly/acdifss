{% extends '@Projet/app.html.twig' %}

{% block body %}
    <h1>Objectifspecifique</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objectifSpecifique.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ objectifSpecifique.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('objectifspecifique_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('objectifspecifique_edit', { 'id': objectifSpecifique.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
